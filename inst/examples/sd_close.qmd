```{r}
library(surveydown)
```

--- page_id

This is a sample survey

```{r}
sd_question(
  type  = 'text',
  id    = 'apple_text',
  label = 'Write a type of apple'
)
```

--- end

This is the end of the survey.

```{r}
sd_close('Exit Survey')
```